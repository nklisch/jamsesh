---
name: finalize
description: Finalize the jamsesh session — opens the portal finalize UI, or prints the cherry-pick plan with --local
argument-hint: "[--local]"
---

Run `bin/jamsesh finalize $ARGUMENTS` and surface the result. Print errors to the user with their exit codes intact.
