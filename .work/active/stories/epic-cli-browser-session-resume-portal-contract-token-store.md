---
id: epic-cli-browser-session-resume-portal-contract-token-store
kind: story
stage: done
tags: [portal, security]
parent: epic-cli-browser-session-resume-portal-contract
depends_on: []
release_binding: null
gate_origin: null
created: 2026-05-30
updated: 2026-05-30
---

# resume_tokens dual-dialect store

Implements **Unit 1** of `epic-cli-browser-session-resume-portal-contract`. See
the feature body for the schema + query shapes.

## Scope

- `db/schema/{sqlite,postgres}.sql`: add the `resume_tokens` table
  (`id, token_hash UNIQUE, session_id, org_id, account_id, issued_at,
  expires_at, used_at`) — mirror `magic_link_tokens`.
- `db/queries/{sqlite,postgres}/resume_tokens.sql`: `CreateResumeToken :one`,
  `GetResumeTokenByHash :one`, and a **winner-returning** `ConsumeResumeToken
  :one` — `UPDATE resume_tokens SET used_at=? WHERE token_hash=? AND used_at IS
  NULL AND expires_at > ? RETURNING *`. Dual-dialect parity per
  `dual-dialect-mirror-queries`. ⚠ Do NOT use `:exec` (the generated `:exec`
  discards rows-affected → concurrent exchanges could double-issue); the row
  returned by `RETURNING` is the single-use winner signal the exchange handler
  relies on.
- Regenerate sqlc; add the store adapter methods in `internal/db/store/`
  (sqlite + postgres adapters, `wrap1`/`wrapList` per `adapter-wrap-helpers`).

## Acceptance criteria

- [x] Dual-dialect parity: identical query names/columns across sqlite + postgres.
- [x] `sqlc generate` clean; `go build ./...` clean.
- [x] `ConsumeResumeToken` is a winner-returning `:one` (RETURNING a row only on
      the first valid consume); a second consume returns no row (single-use), and
      an expired token returns no row. Test concurrent consume → exactly one row.
- [x] Store stores only the token **hash**, never the raw token.
- [x] Adapter methods covered by tests (both dialects where the suite runs them).

## Implementation notes

### Files changed

- `db/schema/sqlite.sql` — added `resume_tokens` table (DATETIME types)
- `db/schema/postgres.sql` — added `resume_tokens` table (TIMESTAMPTZ types)
- `internal/db/migrations/sqlite/00019_resume_tokens.sql` — new goose migration
- `internal/db/migrations/postgres/00020_resume_tokens.sql` — new goose migration (postgres was at 00019 for seq_bigint)
- `db/queries/sqlite/resume_tokens.sql` — CreateResumeToken, GetResumeTokenByHash, ConsumeResumeToken queries (? placeholders)
- `db/queries/postgres/resume_tokens.sql` — same queries ($N placeholders)
- `internal/db/sqlitestore/resume_tokens.sql.go` — generated by `sqlc generate`
- `internal/db/pgstore/resume_tokens.sql.go` — generated by `sqlc generate`
- `internal/db/sqlitestore/models.go` — ResumeToken struct generated
- `internal/db/pgstore/models.go` — ResumeToken struct generated
- `internal/db/sqlitestore/querier.go` — ConsumeResumeToken, CreateResumeToken, GetResumeTokenByHash added
- `internal/db/pgstore/querier.go` — same
- `internal/db/store/store.go` — ResumeToken struct, CreateResumeTokenParams, ConsumeResumeTokenParams, ResumeTokenStore interface, composed into Store + TxStore
- `internal/db/store/sqlite_adapter.go` — sqliteResumeToken converter, sqliteAdapter + sqliteTxStore methods
- `internal/db/store/postgres_adapter.go` — pgResumeToken converter, postgresAdapter + postgresTxStore methods
- `internal/db/store/storetest/storetest.go` — added resume_tokens to Postgres truncateAll
- `internal/db/store/resume_token_test.go` — 7 tests covering create/get, consume winner, single-use, expired, unknown hash, get-not-found, unique hash violation

### Winner-returning consume rationale

`ConsumeResumeToken` uses `:one … RETURNING *` rather than `:execrows`. The
atomic `UPDATE … WHERE token_hash = ? AND used_at IS NULL AND expires_at > ? RETURNING *`
ensures that only the first concurrent caller receives a row back; all subsequent
callers get `sql.ErrNoRows` (translated to `store.ErrNotFound`). This makes the
returned row itself the "winner" signal — the exchange handler (a later story)
calls `ConsumeResumeToken` once and either gets the token data to proceed with
session resumption, or gets `ErrNotFound` and rejects the request, with no
separate read step that would create a TOCTOU window.

### Test results

```
go test ./internal/db/... — all pass (sqlite; postgres requires JAMSESH_TEST_PG_DSN)
ok  jamsesh/internal/db        0.096s
ok  jamsesh/internal/db/store  0.372s
```

Migrations applied count: 19 (sqlite), postgres gains migration 00020.
