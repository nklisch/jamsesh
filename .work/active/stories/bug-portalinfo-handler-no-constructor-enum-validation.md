---
id: bug-portalinfo-handler-no-constructor-enum-validation
kind: story
stage: drafting
tags: [bug, portal, security]
parent: null
depends_on: []
release_binding: null
gate_origin: null
created: 2026-05-25
updated: 2026-05-25
---

# portalinfo Handler accepts arbitrary LandingVariant strings; raw-casts to enum on response

## Discovered by
`gate-tests-portalinfo-handler-invalid-enum-defense` —
`internal/portal/portalinfo/handler_test.go::TestGetPortalInfo_InvalidLandingVariantSurfacesError`.
The test has `t.Skip` entries on the invalid-input subcases pointing at
this backlog id.

## Symptom
`portalinfo.Handler{LandingVariant: "not-a-real-variant"}` is constructed
without complaint, and the `GetPortalInfo` method emits a 200 response
whose `landing_variant` field carries the bogus string verbatim. The
strict-server's response validator does not catch it because the field
is a typed enum on the wire but a plain `string` on the handler struct,
and the handler raw-casts:

```go
LandingVariant: openapi.PortalInfoLandingVariant(h.LandingVariant),
```

at `internal/portal/portalinfo/handler.go:51`.

## Root cause
The handler has no constructor — callers populate `Handler{...}`
literally. Validation is presumed to happen upstream in
`cmd/portal/main.go` reading from validated config, but the type system
doesn't enforce that presumption. A future wiring change that drops the
config-side validation step would silently break the spec contract on
the public bootstrap endpoint.

## Fix direction
Add a `NewHandler(playgroundEnabled bool, landingVariant string)
(*Handler, error)` constructor that calls
`openapi.PortalInfoLandingVariant(landingVariant).Valid()` and returns
`fmt.Errorf("invalid landing_variant %q", landingVariant)` on failure.
Update `cmd/portal/main.go` to call the constructor instead of the
struct literal; surface the error at startup so a misconfigured deploy
fails fast.

After the fix lands, flip the skipped subcases in
`TestGetPortalInfo_InvalidLandingVariantSurfacesError` back to active
assertions (the test currently asserts the desired contract — fail
closed or sentinel — but skips invalid cases linked to this bug).

## Design decisions

- **Constructor signature**: `NewHandler(playgroundEnabled bool,
  landingVariant string) (*Handler, error)`. The variant arrives as
  `string` from config; validation is the constructor's job. Returning
  `(*Handler, error)` lets `cmd/portal/main.go` fail-fast at startup —
  the right place for misconfiguration noise.
- **Validation function**: `openapi.PortalInfoLandingVariant(s).Valid()`
  is already generated by oapi-codegen at
  `internal/api/openapi/server.gen.go:363`. No new validation logic
  needed — reuse the generated checker so the source of truth stays
  the OpenAPI spec.
- **Error message**: `fmt.Errorf("portalinfo: invalid landing_variant %q",
  landingVariant)`. The `%q` quotes the bad value for log clarity; the
  `portalinfo:` prefix matches the existing wrap-style elsewhere in the
  portal packages.
- **Struct field visibility**: Leave the `LandingVariant string` field
  public for now — the constructor enforces validity at construction
  time, and the strict-server's `GetPortalInfo` raw-cast then becomes
  safe by construction. Future hardening could make the field private
  (`landingVariant`) so the only path to setting it is the constructor;
  defer that to a follow-up if test wiring proves brittle, since making
  the field private would force every test fixture that constructs the
  handler literal to switch to the constructor.
- **Caller migration scope**: Single call site in `cmd/portal/main.go`
  (the production wiring). Test fixtures that construct `Handler{...}`
  literally inside `internal/portal/portalinfo/handler_test.go` can stay
  as literals (tests exercise the response shape, not the construction
  invariant) — except for the skipped subcases this story unblocks,
  which now drive the constructor path.

## Acceptance criteria

- [ ] `internal/portal/portalinfo/handler.go` adds
  `NewHandler(playgroundEnabled bool, landingVariant string) (*Handler, error)`
  that:
  - validates `landingVariant` via
    `openapi.PortalInfoLandingVariant(landingVariant).Valid()`
  - returns a typed error with the invalid value quoted when validation
    fails
  - returns a populated `*Handler` on success
- [ ] `cmd/portal/main.go` calls `portalinfo.NewHandler(...)` instead of
  the struct literal at the existing wiring site; the error is checked
  and surfaced at startup
- [ ] The skipped subcases in
  `TestGetPortalInfo_InvalidLandingVariantSurfacesError` are un-skipped
  and exercise the new constructor (assert `NewHandler` returns error,
  `*Handler` is nil)
- [ ] The valid-input subcases continue to pass through the constructor
  path (refactor the table to call `NewHandler` even on the valid rows)
- [ ] `go build ./...` clean
- [ ] `go test ./internal/portal/portalinfo/... ./cmd/portal/... -count 1`
  passes
- [ ] No production startup path can land a `Handler` with an invalid
  `LandingVariant` — verified by `go vet ./...` (no struct literal in
  production code under `cmd/portal/`)

## Test plan

1. Extend `TestGetPortalInfo_InvalidLandingVariantSurfacesError` to
   call `NewHandler` instead of constructing the literal:
   - Invalid rows: assert `err != nil` with substring match on the
     invalid value
   - Valid rows: assert `err == nil`, then run the existing
     response-shape assertions against the constructor-built handler
2. Remove the `t.Skip` calls in the invalid subcases and the linked-bug
   comment.
3. Add one defense test for the misconfiguration-at-startup path: in
   `cmd/portal/main_test.go` (or a smaller package test if main_test
   doesn't fit), assert that an invalid `JAMSESH_LANDING_VARIANT` env
   value causes startup to fail with a clear error mentioning the bad
   value. (If `cmd/portal` has no convenient test harness for env
   parsing, treat this as a follow-up — the constructor unit test is
   the primary trip-wire and is sufficient.)

## Files touched (anticipated)

- `internal/portal/portalinfo/handler.go` — add `NewHandler(...)`
- `cmd/portal/main.go` — switch to constructor call, propagate error
- `internal/portal/portalinfo/handler_test.go` — un-skip subcases,
  retarget table to constructor path
- (Optional) `cmd/portal/main_test.go` — startup-misconfig assertion
