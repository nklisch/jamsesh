# Agile-Workflow Migration Report

Generated by `/agile-workflow:convert` on 2026-05-16.

## Source shape detected

**Greenfield.** No source code, no tracking files (TODO.md / BACKLOG.md /
NOTES.md / tasks/), no workflow-plugin artifacts (docs/designs/ /
docs/ROADMAP.md / docs/PROGRESS.md). Foundation docs in `docs/` are the
input to convert, not legacy tracking to migrate.

The single `ideas.md` at repo root is a one-line scratch note (the
"goal/manifest" concept), already incorporated into the foundation docs.
Left in place as a free-form scratch space; not seeded as a substrate item.

## Foundation docs preserved

The seven foundation docs in `docs/` are untouched by this migration. They
are read-only from convert's perspective and roll forward through
`/agile-workflow:scope`, the `/agile-workflow:*-design` skills, and
`/agile-workflow:implement`.

- `docs/VISION.md` — what jamsesh is and why
- `docs/PRINCIPLES.md` — durable design commitments
- `docs/SPEC.md` — stack, constraints, success criteria, lifecycle
- `docs/ARCHITECTURE.md` — components, data flow, mechanisms
- `docs/PROTOCOL.md` — MCP tools, REST API, schemas, hook contracts
- `docs/SECURITY.md` — trust model, auth, threat model
- `docs/UX.md` — interaction model, flows, portal UI surfaces

## Items seeded

None. Greenfield bootstrap creates an empty `.work/` skeleton with the
expected folder layout. Initial epic decomposition happens next via
`/agile-workflow:epicize`, which reads the foundation docs and produces
epics at `stage: drafting`.

## Files written by this migration

- `.work/CONVENTIONS.md` — project-owned conventions (release mapping, tag
  taxonomy, slug rules, gate config)
- `.work/active/{epics,features,stories}/` — empty in-flight folders
- `.work/backlog/` — empty backlog
- `.work/releases/` — empty release-bundle folder
- `.work/archive/` — empty archive
- `.work/bin/work-view` — query tool copied from plugin
- `.claude/rules/agile-workflow.md` — auto-loaded navigation rules for the
  substrate (loads when editing `.work/` or `docs/`)
- `CLAUDE.md` — created with the agile-workflow section as its body

## Files left untouched

- `docs/` — all seven foundation docs preserved verbatim
- `ideas.md` — kept as scratch space; will be migrated to backlog or
  deleted by the user as they see fit

## Conventions chosen

- **Release mapping:** `tag-based` (git tags name releases, items reference
  via `release_binding:`)
- **Tag taxonomy (10):** `security`, `refactor`, `perf`, `testing`,
  `cleanup`, `documentation` (system-routing) + `plugin`, `portal`, `ui`,
  `infra` (component boundaries)
- **Slug conventions:** kebab-case with parent-prefix
  (e.g., `feature-portal-mcp-endpoint` under `epic-portal`)
- **Stage overrides:** none (master stage progressions)
- **Gate config:** default order
  `[security, tests, cruft, docs, patterns]`

See `.work/CONVENTIONS.md` for the full conventions, including one-line
tag semantics.

## Next steps

1. Run `/agile-workflow:epicize` — reads the foundation docs and seeds
   the initial epic decomposition. For jamsesh this would likely produce
   epics aligned to the major implementation arcs (portal backend, CC
   plugin + local binary, portal UI, auto-merger, finalize flow, infra
   & marketplace).
2. After epicize, review the proposed epics in `.work/active/epics/` and
   adjust their `depends_on` chains if needed.
3. Use `/agile-workflow:autopilot --all` to drain the substrate from
   drafting through implementing → review → done, or work specific epics
   one at a time with `/agile-workflow:autopilot <epic-id>`.
4. As implementation work begins, foundation docs in `docs/` are kept
   current by the design and implement skills — they describe the system
   as it IS, not as it was.

## Reversal

If anything looks wrong: `git revert HEAD` reverts the bootstrap commit.
Source files (foundation docs, ideas.md) are preserved across all paths;
convert never deletes user content.
