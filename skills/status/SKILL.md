---
name: status
description: Show current jamsesh session state — tree summary, peers, scope, mode, unresolved comments addressed to this user
argument-hint: "[--session <id>]"
---

Run `bin/jamsesh status $ARGUMENTS` and surface the result. Print errors to the user with their exit codes intact.
