---
description: Moved — project patterns now live in .agents/skills/patterns/
---

# Pattern Rules (moved)

The project patterns catalog now lives in `.agents/skills/patterns/`
(mirrored at `.claude/skills/patterns/`). Read that catalog's `SKILL.md` index
and the individual pattern files there.
